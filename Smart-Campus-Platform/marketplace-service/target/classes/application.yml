server:
  port: 9083
spring:
  application:
    name: marketplace-service
  datasource:
    # ============ LOCAL DEVELOPMENT (IntelliJ IDEA) ============
    # Uncomment below for running locally on your machine
    url: jdbc:postgresql://localhost:5435/marketdb
    username: market
    password: market
    
    # ============ DOCKER DEPLOYMENT ============
    # Comment below and uncomment above for local development
#    url: jdbc:postgresql://marketplace-db:5432/marketdb
#    username: market
#    password: market
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  rabbitmq:
    # ============ LOCAL DEVELOPMENT (IntelliJ IDEA) ============
    host: localhost
    port: 15673
    
    # ============ DOCKER DEPLOYMENT ============
#    host: rabbitmq
#    port: 15673
app:
  jwt:
    secret: ${JWT_SECRET:TXlfU21hcnRDYW1wdXNfU2VjcmV0X0tleTEyMzQ1Njc4OTAxMjM0NTY3OA==}

